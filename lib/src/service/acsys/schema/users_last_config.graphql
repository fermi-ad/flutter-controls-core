query UsersLastConfig($user: String) {
    usersLastConfiguration(user: $user) {
        configurationId
        configurationName
        channels {
            device
            yMin
            yMax
            lineColor
            markerIndex
        }
        xMin
        xMax
        startTime
        endTime
        timeDelta
        isScalar
        isOneShot
        isShowLabels
        updateDelay
        nAcquisitions
        tclkEvent
        isPersistent
        dataLimit
    }
}